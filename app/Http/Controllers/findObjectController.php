<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;
use App\Models\flat;

class findObjectController extends Controller
{
    public function findObject(Request $request){
      if ($request->only(['typeObject']) = "дома"){

      }
      if ($request->only(['typeObject']) = "квартиры"){

      }
      if ($request->only(['typeObject']) = "участки"){

      }
    }
}
